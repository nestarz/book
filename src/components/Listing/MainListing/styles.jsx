import { Link } from 'gatsby';
import styled from 'react-emotion';

export const Wrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  position: relative;
  z-index:1;
  padding: 1rem 2rem;
  margin-top: 5vh;
  flex: 0 0 50%;
`;
